<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 px-10 py-6 ">
        <a class="text-blue-500 mb-4 inline-block tracking-tight text-[16px] " href="/">Go To Home</a>
        <h1 class="text-3xl text-zinc-200 tracking-tighter font-semibold">All Users</h1>
        <div class="flex w-full py-4 flex-wrap gap-4">
            

            <% if(users.length > 0 && users) { %>
                <% users.forEach((val) => { %>
                    <div class="w-[260px] p-3 rounded-xl bg-zinc-800">
                        <div class="w-full h-[180px] bg-zinc-700 overflow-hidden rounded-xl">
                            <img src="<%= val.url %>" alt="image" class="w-full h-full object-cover object-top">
                        </div>
                        <h1 class="text-xl text-zinc-100 tracking-tighter ml-2 mt-2"> <%= val.name %> </h1>
                        <h1 class="text-[16px] text-zinc-500 tracking-tighter ml-2 "> <%= val.email %> </h1>
                        <div class="flex gap-2 items-center px-2 mt-6">
                            <a class="text-[16px] tracking-tighter text-zinc-500" href="/edit/<%= val._id %>">Edit User</a>
                            <a class="text-[16px] tracking-tighter text-red-500" href="/delete/<%= val._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                    <div>
                        <h2 class="text-[18px] text-zinc-400 inline-block mt-4 capitalize">no users available</h2>
                    </div>
            <% } %>
        </div>
    </div>
</body>
</html>